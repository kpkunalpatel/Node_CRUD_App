version: 0.2

phases: 
  install:
    runtime-versions:
      nodejs: 24.12.0
    commands:
      - echo "We are in the installation phase"
      - npm install

  pre_build:
    commands:
      - echo "We are in the pre test phase"

  build:
    commands:
      -npm run build

  artifacts:
    files:
      - '**/*'

  post_build:
    commands:
      - echo "Build Successfull & Completed"
